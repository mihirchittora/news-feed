import type { Advertisement } from "@/lib/types";

export function AdPreview({ ad, placement = "HOME_BANNER" }: { ad: Partial<Advertisement>; placement?: string }) {
  return <div className="rounded-3xl border border-dashed border-coral/50 bg-paper p-4 sm:p-6"><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-coral">Preview · {placement.replaceAll("_", " ")}</p><div className="mt-4 rounded-2xl border border-line bg-white p-4 shadow-sm"><div className="flex items-center justify-between text-[10px] font-bold uppercase tracking-[0.18em] text-slate"><span>Sponsored</span><span>Advertisement</span></div>{ad.mediaUrl ? <div className="mt-3 overflow-hidden rounded-xl bg-mist">{ad.mediaType === "VIDEO" ? <video src={ad.mediaUrl} poster={ad.thumbnailUrl ?? undefined} controls muted playsInline className="max-h-72 min-h-32 w-full object-cover" /> : <img src={ad.mediaUrl} alt="Advertisement preview" className="max-h-72 min-h-32 w-full object-cover" />}</div> : <div className="mt-3 grid min-h-32 place-items-center rounded-xl bg-mist text-sm font-semibold text-slate">Upload a creative to preview it</div>}<p className="mt-4 text-xs font-bold uppercase tracking-[0.12em] text-coral">{ad.advertiserName || "Advertiser"}</p><h3 className="mt-1 font-display text-2xl font-bold text-ink">{ad.title || "Campaign title"}</h3>{ad.description ? <p className="mt-2 text-sm leading-6 text-slate">{ad.description}</p> : null}{ad.destinationUrl ? <span className="mt-4 inline-flex min-h-10 items-center rounded-xl bg-ink px-4 text-sm font-bold text-white">Learn more</span> : null}</div></div>;
}
